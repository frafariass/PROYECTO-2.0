---ELIMINAR/ELIMINAR COLUMNA ESTADO---
ALTER TABLE ADD ORDEN_COMPRA DROP COLUMN ESTADO;
ALTER TABLE PERFIL DROP COLUMN ESTADO;
ALTER TABLE PRODUCTO DROP COLUMN ESTADO;
ALTER TABLE USUARIO DROP COLUMN ESTADO;
ALTER TABLE VENTA DROP COLUMN ESTADO;
ALTER TABLE ROL DROP COLUMN ESTADO;

ALTER TABLE ORDEN_COMPRA ADD estado_id_estado INTEGER NOT NULL;
ALTER TABLE PERFIL ADD estado_id_estado INTEGER NOT NULL;
ALTER TABLE PRODUCTO ADD estado_id_estado INTEGER NOT NULL;
ALTER TABLE USUARIO ADD estado_id_estado INTEGER NOT NULL;
ALTER TABLE VENTA ADD estado_id_estado INTEGER NOT NULL;
-------------------------------------
---CREAR TABLA ESTADO---
CREATE TABLE ESTADO (
 id_estado INTEGER NOT NULL,
 nombre_estado VARCHAR(20) NOT  NULL
);
ALTER TABLE ESTADO ADD CONSTRAINT estado_pk PRIMARY KEY (id_estado);
----------------------------------------
---ADD FK---
ALTER TABLE ORDEN_COMPRA 
	ADD CONSTRAINT orden_compra_estado_fk FOREIGN KEY (estado_id_estado)
	REFERENCES ESTADO (id_estado);
ALTER TABLE PERFIL
	ADD CONSTRAINT perfil_estado_fk FOREIGN KEY ( estado_id_estado )
	REFERENCES ESTADO ( id_estado );
ALTER TABLE PRODUCTO
	ADD CONSTRAINT producto_estado_fk FOREIGN KEY (estado_id_estado)
	REFERENCES ESTADO (id_estado);
ALTER TABLE USUARIO
	ADD CONSTRAINT usuario_estado_fk FOREIGN KEY (estado_id_estado)
	REFERENCES ESTADO (id_estado);
ALTER TABLE VENTA
	ADD CONSTRAINT venta_estado_fk FOREIGN KEY (estado_id_estado)
	REFERENCES ESTADO (id_estado);